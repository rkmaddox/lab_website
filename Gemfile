source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

# Faster file watching on Windows (used for auto-rebuild during local preview)
gem "wdm", "~> 0.1", platforms: [:mingw, :x64_mingw, :mswin]
